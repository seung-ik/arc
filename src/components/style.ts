'use client';

import styled from 'styled-components';

export const GlobalWrapper = styled.div`
  max-width: 768px;
  margin: 0 auto;
  position: relative;
  overflow: hidden;
  background: rgb(255, 255, 255);
`;
